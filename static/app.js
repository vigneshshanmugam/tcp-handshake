(function(){
	var Packet = function(props){
		this.props = props || {};
	};

	var Network = function(){

	};

	Network.prototype.sendPacket = function(){
		
	};

	Network.prototype.receivePacket = function(){

	};

	// Packet.prototype.getValue = function(key, obj) {
	//     if (typeof obj === 'object') {
	//     	var i, value;
	//     	for (i in obj){
	//     		if(obj.hasOwnProperty(i)){
	//     			if(key === obj.i){ 
	//     				return obj[i];
	//     			}
	//     			if(typeof obj[i] === 'object'){
	//     				value = this.getValue(obj.i)
	//     				if(!!value) return value;
	//     			}
	//     		}
	//     	}
	//     }
	//     return '';
	// };

});